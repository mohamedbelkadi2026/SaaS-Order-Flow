واضح! بغيتي option "Tout sélectionner" تتزاد فالـ dropdown ديال menu (10/25/50/100) — أو تبان مباشرة تحت الـ 100.
هاهو الـ prompt:

FEATURE: Add "Tout sélectionner" option in the page size dropdown
File to change: client/src/pages/orders.tsx only
Problem: User wants to select all orders across all pages. The easiest UX is to add a special option in the existing MENU dropdown (10/25/50/100) that triggers "select all pages".

Change 1 — Add "Tout" option to the page size dropdown
Find the MENU dropdown that has options 10, 25, 50, 100. It looks like:
tsx<Select value={String(pageSize)} onValueChange={(v) => setPageSize(Number(v))}>
  <SelectContent>
    <SelectItem value="10">10</SelectItem>
    <SelectItem value="25">25</SelectItem>
    <SelectItem value="50">50</SelectItem>
    <SelectItem value="100">100</SelectItem>
  </SelectContent>
</Select>
Add a separator and a special "Tout sélectionner" option at the bottom:
tsx<Select
  value={selectAllPages ? "all" : String(pageSize)}
  onValueChange={(v) => {
    if (v === "all") {
      setSelectAllPages(true);
      setSelectedIds(new Set(displayedOrders.map((o: any) => o.id)));
    } else {
      setSelectAllPages(false);
      setSelectedIds(new Set());
      setPageSize(Number(v));
    }
  }}
>
  <SelectContent>
    <SelectItem value="10">10</SelectItem>
    <SelectItem value="25">25</SelectItem>
    <SelectItem value="50">50</SelectItem>
    <SelectItem value="100">100</SelectItem>
    <SelectSeparator />
    <SelectItem value="all" className="text-blue-600 font-semibold">
      ☑️ Tout sélectionner
    </SelectItem>
  </SelectContent>
</Select>

Change 2 — Show a banner when "Tout sélectionner" is active
Just below the filter bar (before the table), add this banner that appears when selectAllPages === true:
tsx{selectAllPages && (
  <div className="flex items-center justify-between bg-blue-50 border border-blue-200 rounded-lg px-4 py-2.5 mb-3">
    <span className="text-sm text-blue-700 font-medium">
      ✅ Toutes les commandes sont sélectionnées
    </span>
    <button
      onClick={() => {
        setSelectAllPages(false);
        setSelectedIds(new Set());
        setPageSize(25);
      }}
      className="text-sm text-red-500 hover:text-red-700 underline font-medium"
    >
      Annuler
    </button>
  </div>
)}

Change 3 — Add the missing state (if not already added)
If selectAllPages state doesn't exist yet, add it near selectedIds:
tsconst [selectAllPages, setSelectAllPages] = useState(false);

Change 4 — Reset on filter/tab change
Find all places where setSelectedIds(new Set()) is called (filter changes, tab changes, search changes). Add setSelectAllPages(false) next to each one.

